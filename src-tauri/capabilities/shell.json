{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "shell-windows",
  "description": "Full IPC for bundled shell pages (splash, error, preferences)",
  "windows": ["splash", "error", "prefs"],
  "permissions": [
    "core:default",
    "store:default",
    "notification:default",
    "opener:default"
  ]
}
